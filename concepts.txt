
let node = Grapheme.parseString("3+cos(pi)")

Grapheme.defineVariable('x', node) // Should throw: x is reserved
Grapheme.defineVariable('i', node) // Should throw: i is reserved
Grapheme.defineVariable('a', node) // Okay



Grapheme.defineFunction('sinh', parseString('cosh(x)'), {'x': 'complex'}) // Should throw: sinh is reserved
Grapheme.defineFunction('f', parseString('cosh(x)'), {'x': 'complex'})
Grapheme.defineFunction('g', parseString('p(x)'), {'x': 'complex'})

Grapheme.defineFunction('h', parseString('g(x)'), {'x': 'complex'})
Grapheme.defineFunction('p', parseString('h(x)'), {'x': 'complex'}) // Should throw: circular dependencies
