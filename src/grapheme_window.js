/** A grapheme window is an actual viewable instance of Grapheme.
That is, it is a canvas that can be put into the DOM and manipulated.

Properties:

*/
class GraphemeWindow {
  constructor(context) {


  }
}

export {GraphemeWindow as Window};
