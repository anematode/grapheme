import { OperatorNode } from '../node'

const product = (node1, node2) => {
  return
}

const productDerivative = (variable, node1, node2) => {
  return new OperatorNode({
    operator: '+',
    children: [

    ]
  })
}
